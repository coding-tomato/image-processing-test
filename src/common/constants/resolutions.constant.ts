/**
 * Constants for image resolutions
 */
export enum Resolution {
  LARGE = '1024',
  MEDIUM = '800',
}

// Export resolution values as an array for iteration
export const RESOLUTIONS = Object.values(Resolution);
